CREATE TABLE admin (
    id INTEGER PRIMARY KEY,
    email TEXT,
    password TEXT,
    first_name TEXT,
    surname TEXT,
    age INTEGER,
    gender TEXT,
    contact_number INTEGER,
    profile_text TEXT,
);