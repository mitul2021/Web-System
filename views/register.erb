<html lang="en">
    <head>
        <title>Register</title>
        <link href="<%= url('/style.css') %>" rel="stylesheet" type="text/css" />
    </head>
    <body>
        <h1>Register</h1>
        <p>Register with your credentials below:</p>
        
        <form method="post" action="/register">
            
            <label>Enter Username:</label><br>
            <input type="text" name="username" placeholder="Enter your desired Username here..." value="<%= h @user.username unless @user.nil? %>"/><br>
            
            <% if !@user.nil? &&  !@user.errors.nil? && @user.errors.include?("username") %>
                <% @user.errors["username"].each do |error| %>
                    <p class="error"><strong><%= error %></strong></p>
                <% end %>
            <% end %>
            <br>
            
            
            <label>Enter Email:</label><br>
            <input type="email" name="email" placeholder="Enter your desired Email here..." value="<%= h @user.email unless @user.nil? %>"/><br>
            
            <% if !@user.nil? &&  !@user.errors.nil? && @user.errors.include?("email") %>
                <% @user.errors["email"].each do |error| %>
                    <p class="error"><strong><%= error %></strong></p>
                <% end %>
            <% end %>
            <br>
            
            
            <label>Password:</label><br>
            <input type="password" name="password" placeholder="Enter your desired Password here..." value="<%= h @user.password unless @user.nil? %>"/><br>
            
            <% if !@user.nil? &&  !@user.errors.nil? && @user.errors.include?("password") %>
                <% @user.errors["password"].each do |error| %>
                    <p class="error"><strong><%= error %></strong></p>
                <% end %>
            <% end %>
            <br>
            
            
            <label>Repeat Password:</label><br>
            <input type="password" name="password_repeat" placeholder="Repeat your Password here..." value="<%= h @user.password_repeat unless @user.nil? %>"/><br>
            
            <% if !@user.nil? && !@user.errors.nil? && @user.errors.include?("password_repeat") %>
                <% @user.errors["password_repeat"].each do |error| %>
                    <p class="error"><strong><%= error %></strong></p>
                <% end %>
            <% end %>
            <br>
            
            
            <label>Mentee:</label>
            <input type="radio" name="user_type" id="mentee" value="mentee" checked/>
            <br>
            
            <label>Mentor:</label>
            <input type="radio" name="user_type" id="mentor" value="mentor"/>
            <br>
            
            <label>Admin:</label>
            <input type="radio" name="user_type" id="admin" value="admin"/>
            <br><br>
                
<!--             do some comparison and validation if the typed password meets the minimum requerements -->
            <input type="submit" value="Register" class="button" name="submit_register"/>
            <br>
            
            <a href="/login" class="button">Already have an account? Login here!</a>
            
        </form>
        
        
    </body>
</html>